{
    "version": 2,
    "builds": [
        {
            "src": "cmd/api/*.go",
            "use": "@vercel/go"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "cmd/api/$1"
        }
    ]
}